import styled from 'styled-components';

const Card = styled.div`
  width: fit-content;

  border-radius: 12px;
  background-color: #F5FCFF;

  box-shadow: -2px 2px 50px 0px rgba(152, 152, 152, 0.2);
`;

export default Card;
